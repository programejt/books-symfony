{% block stylesheets %}
  <link href="{{ asset('styles/form.css') }}" rel="stylesheet">
{% endblock %}

{{ form_start(form, {
  'attr': {
    'id': 'author-form',
    'class': 'container content-container data-container'
  }
}) }}
  {{ include('forms/global_errors.html.twig') }}
  {{ include('forms/field.html.twig', {
    'field': form.name
  }) }}
  {{ include('forms/field.html.twig', {
    'field': form.surname
  }) }}
  {{ form_rest(form) }}
  {{ include('forms/form_buttons.html.twig') }}
{{ form_end(form) }}
